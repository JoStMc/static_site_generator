# Static Site Generator
Markdown to HTML in Python. Static content in `static` and markdown in `content` (copied to `docs`).

`main.sh` to run locally on port 8888 and `build.sh` for production – a demo can be found in the pages for [this repo](https://jostmc.github.io/static_site_generator/).
